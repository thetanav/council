I want to have a /agent route where there is a ai chat with agent-browser use case, with agent-browser agent can use the browser it also sends the screenshot after ever change on the screen to the web ui, these will be like manus clone but for local browser with agent-browser utility, you can use ai sdk for ai chat inference, 

the ui will has two panes left pane shows the chat, right pane show the screent shots of the browser it is using and show the thinking in a scrollable fashion
